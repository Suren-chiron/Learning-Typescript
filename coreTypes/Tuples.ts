// info: Tuples type

const tuplesArray: [string, number] = ['mohammad', 3];

const memberId: {
   name: string;
   family: string;
   one: [string, number];
} = {
   name: 'mohammad',
   family: 'karimi',
   one: ['mohammad', 3],
};
