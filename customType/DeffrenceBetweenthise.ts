// ? we use Interface for object or function and son on
// ? we use type Alias for union type, literal type and some basic type like string and number and boolean

// ? it's better that we use Interface for object and function maybe array
// ? and it's better that we use Type Alias for union type, literal type and some basic type and maybe array
